# Dependencies
node_modules/

# Build / test artifacts
.screenshots/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local Claude permissions (per-user, not project-shared)
.claude/settings.local.json

# Scaffolded client sites — each one becomes its own project/repo
# once the team takes it over. Not tracked in the marketing-site repo.
clients/
